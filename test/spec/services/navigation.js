// jshint indent:4
(function(){
    
    'use strict';

    describe('Service: Navigation', function () {
        
        // load the service's module
        beforeEach(module('tofuForumApp'));
        
        // instantiate service
        var Navigation;
        beforeEach(inject(function (_Navigation_) {
            Navigation = _Navigation_;
        }));
        
        it('Go to a Url', function () {
 
        });

        it('Go to the default Url', function () {
 
        });

        it('Go to a safe route', function () {
 
        });

        it('Go to a protected route', function () {
 
        });

        it('Add a safe route', function () {
 
        });

        it('Remove a safe route', function () {
 
        });
        
    });

})();
